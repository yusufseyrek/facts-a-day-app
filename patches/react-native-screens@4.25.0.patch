diff --git a/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/container/TabsContainer.kt b/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/container/TabsContainer.kt
index 3342c4161c27a5a009c1f4e5db5e4c5c11724cb3..356b524b691ab6e40dcb63fe224e1b7f09bd41f4 100644
--- a/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/container/TabsContainer.kt
+++ b/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/container/TabsContainer.kt
@@ -275,6 +275,12 @@ class TabsContainer internal constructor(
     override fun onDetachedFromWindow() {
         super.onDetachedFromWindow()
         teardownFragmentManager()
+        // Backported from react-native-screens main: without this, the next
+        // re-attach (e.g. returning from a pushed/modal screen) calls setup()
+        // on an already-set-up coordinator and throws IllegalStateException,
+        // crashing the app ("setup method must not be called again without
+        // calling teardown() first").
+        colorSchemeCoordinator.teardown()
     }
 
     override fun onConfigurationChanged(newConfig: Configuration?) {
